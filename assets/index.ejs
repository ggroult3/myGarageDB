<!DOCTYPE html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width = display-width, initial-scale = 1">
    <meta name="author" content="Guillaume Groult et Antoine Valentin">
    <meta name="description" content="Projet MOD 4.6 sur MySQL">
    <title>MyGarageDB : Projet MOD 4.6</title>
</head>

<body>
    <h1>Bienvenue à MyGarageDB</h1>
    <p>Salut !</p>
    <br/><br/>
    <form action="/mysql" method="POST">
        <label for="submitSQLRequest">Cliquer sur le bouton Envoyer pour charger la table Administrateur</label>
        <input id="submitSQLRequest" type="submit"/>
    </form>
    <% console.log("\nFélicitations, vous êtes connecté à la page principale !\n") %> 
    <% if (JSON.stringify(result)!=JSON.stringify([])) { %>
        <h3 id='table'>Table Administrateur</h3>
        <table id='table'>
            <thead>
                <tr>
                    <th colspan="1">idadm</th>
                    <th colspan="1">nom</th>
                    <th colspan="1">prenom</th>
                </tr>
            </thead>
            <tbody>
                <% result.forEach(element => { %>
                    <tr>
                        <td><%= element.idadm %> </td>
                        <td><%= element.nom %></td>
                        <td><%= element.prenom %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %>
</body>